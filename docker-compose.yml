version: '2'

volumes:
  cache:
    driver: local

services:
  app:
    image: 'yumeinaruu/iis:v3'
    build:
      context: .
    container_name: integrated_system
    ports:
      - "8080:8080"
    depends_on:
      - db
      - redis
    networks:
      - iis_network

  db:
    image: 'postgres:16.1'
    container_name: frosty_ride
    ports:
      - "5432:5432"
    volumes:
      - ./data/db:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=123
      - POSTGRES_DB=iis_db
    networks:
      - iis_network
  redis:
    image: 'redis:latest'
    restart: always
    container_name: redis
    ports:
      - "6379:6379"
    command: redis-server --save 20 1 --loglevel warning --requirepass cm9vdA==
    volumes:
      - cache:/data
    networks:
      - iis_network


networks:
  iis_network:
    driver: bridge
